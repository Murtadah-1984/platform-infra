---
# Notification Service Configuration
replicaCount: {{ .Values.global.replicaCount | default 2 }}

image:
  repository: {{ .Values.global.imageRegistry }}/notification
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
  hosts:
    - host: notification.{{ .Values.global.domain }}
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: notification-tls
      hosts:
        - notification.{{ .Values.global.domain }}

resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70

env:
  - name: ASPNETCORE_ENVIRONMENT
    value: "Production"
  - name: ASPNETCORE_URLS
    value: "http://+:8080"
  - name: RabbitMQ__ConnectionString
    valueFrom:
      secretKeyRef:
        name: notification-secrets
        key: rabbitmq-connection
  - name: Email__SmtpHost
    valueFrom:
      secretKeyRef:
        name: notification-secrets
        key: smtp-host
  - name: Email__SmtpPort
    value: "587"

livenessProbe:
  httpGet:
    path: /health
    port: 8080
  initialDelaySeconds: 30
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /health/ready
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 5

serviceAccount:
  create: true

podSecurityContext:
  fsGroup: 2000
  runAsNonRoot: true
  runAsUser: 1000

securityContext:
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    namespace: monitoring

